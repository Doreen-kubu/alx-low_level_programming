#include <stdio.h>
/**
 * main: oh dear don't we love redundancies
 * return: 0 another day
 */
int main(void) /*main = starts reading, return = 0*/
{
printf("with proper grammar, but the outcome is a piece of art,\n ");
return (0);
}
