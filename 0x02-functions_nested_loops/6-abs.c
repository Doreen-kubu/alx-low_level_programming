#include "main.h"

/**
 * _abs - Entry point
 * @n - integer
 * print the absolute value of an intenger n
 * return int
 */

int _abs(int n)
{
if (n < 0)
return (-n);

else if (n > 0)
return (n);

else
return (n);
}
