#include "main.h"

/**
 * print_last_digit - return last digit
 * @n : number to check
 * Return:0 or 1
 */

int print_last_digit(int)
{
int y; /* the last digit */
int n;
y = n % 10;
if (n < 0)
y = y * -1;
_putchar('y');
return (y);
}
