#include "main.h"
/**
 * print_last_digit(int): entry point
 * prints the last digit of a number
 * returns the last digit
 */
int print_last_digit(int)
{
int y; /* the last digit */
y = n % 10;
if (n < 0)
y = y * -1;
_putchar('y');
return (y);
}
