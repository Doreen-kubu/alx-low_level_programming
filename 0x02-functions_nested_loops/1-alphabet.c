#include "main.h"
/**
 * print_alphabet - prints the alphabet in lower case
 * Return - void always 0
 */
void print_alphabet(void)
{
int i;
for (i = 'a'; i <= 'z'; i++)
_putchar(i);
_putchar('\n');
return (0);
}
